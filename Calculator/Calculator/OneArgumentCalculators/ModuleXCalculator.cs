using System;

namespace Calculator.OneArgumentCalculators
{
    public class ModuleXCalculator
    {
        public double Calculate(double first)
        {
            return Math.Abs(first);
        }
    }
}
